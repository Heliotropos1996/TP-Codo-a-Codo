var button = document.getElementById("ticketsBtn");

// Agregar un evento de clic al botón
button.addEventListener("click", function () {
    // Redirigir a otro archivo HTML
    window.location.href = "tickets.html";
});